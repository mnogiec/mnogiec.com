export type Props = {
  children: React.ReactNode,
  header?: boolean,
  lang: string,
  pageTitle: string,
  pageDescription: string,
  pageKeywords: string,
}

export type CSSProps = {
  header?: boolean,
  sideMenu?: boolean,
  showSideMenu?: boolean
}
