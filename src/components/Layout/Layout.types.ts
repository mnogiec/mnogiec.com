export type Props = {
  children?: React.ReactNode,
  pageTitle: string,
  header?: boolean,
  lang: string,
}

export type CSSProps = {
  header?: boolean,
  sideMenu?: boolean,
  showSideMenu?: boolean
}
